# Request: MB-POST 000001-000010 reanalysis pilot

Help me plan and, only after explicit approvals, execute the supported
untargeted MS-DIAL reanalyses represented by MB-POST accessions `MPST000001`
through `MPST000010`.

Start by running the compatibility audit in
`prompts/01-compatibility-audit.md`. If it returns `FAIL`, stop before any raw
download and provide the feedback package for Codex/MS-DIAL developers.

If the audit passes:

1. Ask me what scientific question and comparison this reanalysis should answer,
   whether annotation or comparative profiling is central, and which outputs I
   need. Preserve the answer as `analysis_purpose`; do not infer it from the title,
   and pass it explicitly to every Interactive batch, plan, and download call.
2. Enumerate all analysis units rather than assuming one run per accession.
3. Use `D:\13_MSDIAL_Public_Reanalysis\analysis` explicitly as
   `workspace_root` for every
   plan and download. All raw data, provenance, MS-DIAL processing files, and
   generated results must remain on D:.
4. Exclude targeted SRM/MRM, SIM, GC-MS, DI-MS, imaging, product-ion-only, and other
   unsupported units. Put uncertain records in `requires_review`; do not guess.
5. For each eligible untargeted LC-MS/MS DDA or DIA/AIF/SWATH unit, preserve its exact file manifest,
   polarity, acquisition mode, sample metadata, source subrecord, and repository
   provenance. Relay Catalog results to Interactive by `handoff_path`; never
   inline or truncate full file/sample manifests.
6. Propose a purpose-specific MS-DIAL Class hierarchy and contrast from sample
   metadata. Show missingness, confounding, sample roles, QC/blank assignment,
   batch, and analytical order. Do not save it until I approve it.
7. Prepare a batch manifest containing unit ID, accession, method, sample/file
   counts, bytes, warnings, output directory, raw-retention policy, and expected
   analysis/QA/publication artifacts.
8. Default to keeping raw data. Report selected-unit bytes separately from the
   actual repository-bundle bytes, and ask me to approve the selected units,
   workspace, bundle-aware size bound, and retention policy.
9. Process approved units sequentially. Before each production run, show the
   generated MS-DIAL command and ask for confirmation. Never reuse an output
   directory with incompatible existing files.
10. Use raw-header preflight when polarity, DDA versus DIA/AIF/SWATH, MS/MS
    presence, or project type remains uncertain. Never silently convert an
    unknown, targeted, or DI-MS unit into LC-MS DDA.
11. Before each production run, execute the zero-threshold single-file
    diagnostic. Let Interactive choose a mid-run QC, or a mid-run non-blank
    sample when no QC exists. Estimate Minimum peak height for 3,000-6,000
    retained peaks using the returned 100-unit QTOF or 1,000-unit FT step. Keep
    0 when no more than 6,000 peaks are detected. Use
    `TimeBasedLinearWeightedMovingAverage` and retain the complete tuning record.
12. For annotation, ask whether to use the local tiered LBM + private MSP
    pipeline. Never copy private MSP files into output bundles.
13. Derive internal-standard QA targets only from repository evidence and
    reviewed chemical/adduct information. Leave RT blank when not declared.
14. For each completed unit, validate and preview mzTab-M, generate evaluable QA,
    create Materials and Methods plus supplementary tables, and retain the exact
    job-scoped artifact inventory.
15. Produce a final matrix of completed, excluded, review-blocked, and failed
    units. Include storage used, raw files retained/deleted, and structured
    source-code feedback using `feedback/claude-audit-template.md`.

Do not label choices as "Recommended". Present evidence and tradeoffs neutrally.
